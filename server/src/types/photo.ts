interface Photo {
  id?: number;
  title: string;
  place: string;
  image: string;
  user_account_id?: number;
}

interface newPhoto {
  id?: number;
  title: string;
  image: string;
  place: string;
}

interface photoId {
  id?: number;
}
